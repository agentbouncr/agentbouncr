{
  "name": "agentbouncr",
  "private": true,
  "license": "Elastic-2.0",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/sqlite",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "check:isolation": "cd packages/core && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.20.0",
    "glob": "^13.0.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.24.0",
    "vitest": "^3.0.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
